# Markdown 記法ルール

AI エディター使用時の Markdown 記法に関するルール集

## 基本書式

- 箇条書きのシンボルは `-` を使用 (`*` は使わない)
- `#` はファイル内に単一で使用
- `##` や `###` の次の行には空行を開ける
- 見出しやリストで連番は使用しない (アイテムの追加・変更時に更新が面倒なため)

## 文字表記

- 半角と日本語の間にはスペースを開ける
- カッコは `()` 半角のものを使用
- 強調 (`**` `**`) は無意味に使用しない
  - 特別に強調するためにのみ使用する
